# Docker Compose for integration test infrastructure
#
# Spins up an SSH server with Java 21 and an example app (math-game.jar).
# This allows integration tests to run in a self-contained environment
# without relying on a specific external server.
#
# Usage:
#   docker compose -f docker-compose.test.yml up --build -d
#   export TEST_SSH_HOST=localhost
#   export TEST_SSH_USER=testuser
#   export TEST_SSH_PASSWORD=testpass
#   pytest tests/integration/ -m integration -v
#   docker compose -f docker-compose.test.yml down
#
# In CI (GitHub Actions):
#   services:
#     test-target:
#       image: narcissux/arthas-test-target:latest
#       ports: ['2222:22']

services:
  test-target:
    build:
      context: .
      dockerfile: Dockerfile.test-target
    ports:
      - "2222:22"
    environment:
      - SSH_USER=testuser
      - SSH_PASSWORD=testpass
    healthcheck:
      test: ["CMD-SHELL", "sshd -t && pgrep -u testuser -f '[m]ath-game' >/dev/null"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    privileged: true   # Required for Java attach (ptrace)
    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp:unconfined

  # Same real fixture on a separate SSH port, for multi-target isolation tests.
  test-target-b:
    build:
      context: .
      dockerfile: Dockerfile.test-target
    ports:
      - "2223:22"
    environment:
      - SSH_USER=testuser
      - SSH_PASSWORD=testpass
    healthcheck:
      test: ["CMD-SHELL", "sshd -t && pgrep -u testuser -f '[m]ath-game' >/dev/null"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    privileged: true
    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp:unconfined
